using UnityEngine;
using System.Collections;
/*
//class from json2csharp
public class Stats 
{

    public int exp { get; set; }
    public double gp { get; set; }
    public int hp { get; set; }
    public int lvl { get; set; }
    public int toNextLevel { get; set; }
    public int maxHealth { get; set; }
	
}


//code from uvwxy client habitconnectionV1 converted by tangible software java to c# client
private JSONArray fetchTasks(string type)
{
		try
		{
			JSONArray t = new JSONArray(getJSONResponse("/api/v1/user/tasks?type=" + type));
			return t;
		}
		catch (Exception)
		{
			return null;
		}
}


public virtual void fetchHabits(HabitDataV1 target)
{
		target.clobberTasks(fetchTasks("habit"));
}

	public virtual void fetchDailies(HabitDataV1 target)
	{
		target.clobberTasks(fetchTasks("daily"));
	}

	public virtual void fetchTodos(HabitDataV1 target)
	{
		target.clobberTasks(fetchTasks("todo"));
	}

	public virtual void fetchRewards(HabitDataV1 target)
	{
		target.clobberTasks(fetchTasks("reward"));
	}


*/